# spreadsheet-kit

Shared core spreadsheet primitives for agent-facing surfaces.

This crate currently provides:

- Path normalization helpers
- Edit normalization and application helpers
- Shared session runtime trait scaffold
- Shared core value types

Used by:

- `spreadsheet-mcp` (stateful MCP server)
- `agent-spreadsheet` (stateless CLI)
